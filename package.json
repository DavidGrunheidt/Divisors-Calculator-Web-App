{
  "name": "node-react",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && nodemon --exec npm start",
    "server": "cd server && nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "@material-ui/core": "^1.5.1",
    "express": "^4.17.3",
    "mongodb": "^3.5.5",
    "react": "^16.13.0",
    "react-dom": "^16.13.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.0",
    "nodemon": "2.0.2"
  }
}
